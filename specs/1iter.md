v1

1.the project is a product for developers to view analytics rather than plain analtic int futre versions we will add some change the primse remains the same

2.the premi0se of the app is to build an web based analytics for any website via a screen code insertion in the developers codebase

3.the code should return tha ip approximate locations of the viewers the developer should be recommened to ensure a discailmer or cookies notifaction for the user to share the info

4.the info from multiple users can be stroesd in postgress supabase and the derived analytics should then be shown in a dashbord

5.the developers should be able to login via supabase auth and register their website with whatever url and the code for snipeing should be provided during the registration process

satch nextjs and react use nextjs for backend too use tailwind and anything else for better results

this is just an v1 eventually we will expand this more

make a env to store ur varibales which i will edit or change accordingly
